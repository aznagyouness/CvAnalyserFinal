# Core FastAPI stack
#fastapi	0.128.0: Completely dropped support for pydantic.v1. Your data models and validation code must now be fully compatible with Pydantic v2's native API.
fastapi==0.136.3
uvicorn[standard]==0.40.0
python-multipart==0.0.32
python-dotenv==1.2.1
pydantic==2.12.5
pydantic-settings==2.12.0


# File handling
aiofiles==25.1.0

# AI/ML stack
# for langchain and related dependencies
langchain==1.2.12
langchain-community==0.4.1
langchain-text-splitters==1.1.1
PyMuPDF==1.27.2.2
# models provider
openai==2.30.0
cohere==5.20.7
qdrant-client==1.17.1
nltk==3.9.3
tiktoken==0.9.0
aiolimiter==1.2.1

# MongoDB stack
# motor is used for async MongoDB operations
# pymongo is the underlying driver for MongoDB, they are compatible
motor==3.4.0
pymongo==4.6.3
# Consider removing pydantic-mongo if it causes issues
# pydantic-mongo==2.3.0


# Fixed dependencies to resolve Docker warnings
urllib3>=2.2.1,<3.0.0  # Resolves RequestsDependencyWarning
chardet>=5.2.0,<6.0.0   # Resolves RequestsDependencyWarning
requests>=2.31.0        # Ensures compatibility with urllib3/chardet

# Database dependencies
SQLAlchemy==2.0.46
asyncpg==0.31.0
alembic==1.18.1
psycopg2==2.9.11
pgvector==0.4.2


# logging better then logging library
structlog==26.1.0

# Monitoring and metrics
prometheus-client==0.24.1
starlette-exporter==0.23.0
fastapi-health==0.4.0



# Taskiq stack
# don't install taskiq[reload] in production it's bad for production, ask deepseek
taskiq==0.12.4
taskiq-aio-pika==0.6.0
taskiq-redis==1.2.2
taskiq-fastapi==0.5.0
taskiq[metrics,orjson,uv]==0.12.4
taskiq-pipelines==0.1.4
taskiq-dashboard==0.4.4


# for celery task queue and Background Processing
#celery[librabbitmq,redis]==5.6.2

# For monitoring celery 
#flower==2.0.1

